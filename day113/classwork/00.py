hi = [1,2,4,5]
hi.append(6) #adds an element at the end
hi.insert(3, 3) # adds an element based on which index
max(hi) #outputs the highest element
min(hi) #outputs the lowest element in the list
hi.sort #outputs elements in order

bye = (1,2,3,4)